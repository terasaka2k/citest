test:
  override:
    - /bin/true

deployment:

  lab:
    tag: /^lab-\d.*/
    commands:
      - echo "hacking"
      - git branch
      - git describe --tags
      - test -f circle.yml

      - python --version
