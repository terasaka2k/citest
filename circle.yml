test:
  override:
    - /bin/true

deployment:
  lab:
    branch: lab
    commands:
      - echo "hacking"
      - git branch
      - git describe --tags
  release:
    branch: master
    tag: /^release-\d.*/
    commands:
      - echo "releasing"
      - git branch
      - git describe --tags
