test:
  override:
    - /bin/true

deployment:

  lab:
    branch: lab
    tag: /^lab-\d.*/
    commands:
      - echo "hacking"
      - >-
        print hi
        by
      - git branch
      - git describe --tags

      - python --version

  release:
    tag: /^release-\d.*/
    commands:
      - echo "releasing"
      - git branch
      - git describe --tags

      - python --version
