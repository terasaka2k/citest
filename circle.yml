test:
  override:
    - /bin/true

deployment:

  lab:
    tag: /^lab-\d.*/
    commands:
      - echo "hacking"
      - >-
        print hi
        by
      - git branch
      - git describe --tags

  release:
    tag: /^release-\d.*/
    commands:
      - echo "releasing"
      - git branch
      - git describe --tags
      - [[ 1 ]]
